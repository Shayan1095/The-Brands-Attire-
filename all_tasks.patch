*** Begin Patch
*** Update File: C:\Users\mfabid\My stack\projects\ZCodeProject\shared.css
*** Find:
/* ---------- REVEAL ---------- */
.reveal { opacity: 0; transform: translateY(24px); transition: opacity 600ms var(--ease), transform 600ms var(--ease); }
.reveal.in { opacity: 1; transform: none; }
@media (prefers-reduced-motion: reduce){ .reveal { opacity: 1; transform: none; transition: none; } }
*** Replace:
/* ---------- REVEAL ---------- */
.reveal { opacity: 0; transform: translateY(24px); transition: opacity 600ms var(--ease), transform 600ms var(--ease); }
.reveal.in { opacity: 1; transform: none; }
.reveal:nth-child(1)  { transition-delay: 0ms; }
.reveal:nth-child(2)  { transition-delay: 80ms; }
.reveal:nth-child(3)  { transition-delay: 160ms; }
.reveal:nth-child(4)  { transition-delay: 240ms; }
.reveal:nth-child(5)  { transition-delay: 320ms; }
.reveal:nth-child(6)  { transition-delay: 400ms; }
.reveal:nth-child(7)  { transition-delay: 480ms; }
.reveal:nth-child(8)  { transition-delay: 560ms; }
.reveal:nth-child(9)  { transition-delay: 640ms; }
.reveal:nth-child(10) { transition-delay: 720ms; }
@media (prefers-reduced-motion: reduce){ .reveal { opacity: 1; transform: none; transition: none; } }
*** End Patch

*** Begin Patch
*** Update File: C:\Users\mfabid\My stack\projects\ZCodeProject\shared.css
*** Find:
.hero__title {
  color: #F4F1EC;
  font-size: var(--t-hero);
  padding: var(--s-10) var(--gutter) var(--s-7);
  margin: 0; max-width: 16ch;
}
*** Replace:
.hero__title {
  color: #F4F1EC;
  font-size: var(--t-hero);
  padding: var(--s-10) var(--gutter) var(--s-7);
  margin: 0; max-width: 16ch;
  opacity: 0;
  transform: translateY(20px);
  animation: heroFadeUp 700ms var(--ease) forwards;
}
.hero__title + .hero__meta,
.hero__meta {
  opacity: 0;
  transform: translateY(20px);
  animation: heroFadeUp 700ms var(--ease) forwards;
  animation-delay: 150ms;
}
@keyframes heroFadeUp {
  to { opacity: 1; transform: none; }
}
*** End Patch

*** Begin Patch
*** Update File: C:\Users\mfabid\My stack\projects\ZCodeProject\shared.css
*** Find:
.pdp__main img { width: 100%; height: 100%; object-fit: cover; transition: opacity var(--dur) var(--ease); }
.pdp__main img { width: 100%; height: 100%; object-fit: cover; transition: opacity var(--dur) var(--ease); }
*** Replace:
.pdp__main img { width: 100%; height: 100%; max-height: 520px; object-fit: cover; transition: opacity var(--dur) var(--ease); }
*** End Patch

*** Begin Patch
*** Update File: C:\Users\mfabid\My stack\projects\ZCodeProject\shared.css
*** Find:
/* ---------- UTIL ---------- */
.text-center { text-align: center; }
.mt-0{margin-top:0} .mt-4{margin-top:var(--s-4)} .mt-6{margin-top:var(--s-6)} .mt-8{margin-top:var(--s-8)}
.hide-mobile {  }
@media (max-width: 760px){ .hide-mobile { display: none; } }
*** Replace:
/* ---------- UTIL ---------- */
.text-center { text-align: center; }
.mt-0{margin-top:0} .mt-4{margin-top:var(--s-4)} .mt-6{margin-top:var(--s-6)} .mt-8{margin-top:var(--s-8)}
.hide-mobile {  }
@media (max-width: 760px){ .hide-mobile { display: none; } }

/* ---------- TOUCH ACTIVE (Task 5) ---------- */
@media (hover: hover) {
  .card:hover,
  .world:hover,
  .btn:hover { /* keep existing hover styles */ }
}
@media (hover: none) {
  .card:active,
  .world:active,
  .btn:active { transform: translateY(-6px); box-shadow: 0 18px 36px -12px rgba(0,0,0,.28); }
  .card__add:active { opacity: 1; transform: translate(-50%, 0); }
  .card__wish:active { background: var(--signal-soft); border-color: var(--signal); color: var(--signal); transform: scale(1.08); }
  .icon-btn:active { background: var(--ink); color: var(--paper); border-color: var(--ink); }
  .filter-chip:active { border-color: var(--ink); }
  .size:active { border-color: var(--ink); }
  .card__details:active { background: var(--ink); color: var(--paper); border-color: var(--ink); }
  .wish-btn:active { border-color: var(--signal); color: var(--signal); }
  .qty button:active { background: var(--bone); }
  .pdp__thumb:active { border-color: var(--signal); }
  .acc__head:active { color: var(--signal); }
  .modal__close:active { background: var(--ink); color: var(--paper); }
  .search-close:active { background: var(--ink); color: var(--paper); }
  .search-chip:active { border-color: var(--signal); color: var(--signal); }
  .search-result:active { border-color: var(--ink); transform: translateX(4px); }
  .item .rm:active { color: var(--signal); }
  .drawer__foot .btn:active { background: var(--signal); border-color: var(--signal); }
  .footer-grid a:active { opacity: 1; color: var(--signal); }
}
*** End Patch

*** Begin Patch
*** Update File: C:\Users\mfabid\My stack\projects\ZCodeProject\shared.js
*** Find:
  // scroll reveal
  const io = new IntersectionObserver((entries)=>{
    entries.forEach(en=>{ if(en.isIntersecting){ en.target.classList.add("in"); io.unobserve(en.target); } });
  }, { threshold:.12 });
  document.querySelectorAll(".reveal").forEach(el=>io.observe(el));
*** Replace:
  // scroll reveal
  const io = new IntersectionObserver((entries)=>
    entries.forEach(en=>{
      if(en.isIntersecting){
        const siblings = Array.from(en.target.parentElement.querySelectorAll(".reveal"));
        const idx = siblings.indexOf(en.target);
        if (idx >= 0) en.target.style.transitionDelay = `${idx * 80}ms`;
        en.target.classList.add("in");
        io.unobserve(en.target);
      }
    })
  , { threshold:.12 });
  document.querySelectorAll(".reveal").forEach(el=>io.observe(el));
*** End Patch

*** Begin Patch
*** Update File: C:\Users\mfabid\My stack\projects\ZCodeProject\product.html
*** Find:
  // gallery thumbnails
  document.getElementById("thumbs").addEventListener("click", e=>{
    const t = e.target.closest("[data-i]");
    if(!t) return;
    activeImg = +t.dataset.i;
    document.getElementById("mainImg").src = p.images[activeImg];
    document.querySelectorAll(".pdp__thumb").forEach(th=>th.classList.toggle("active", th===t));
  });
*** Replace:
  // gallery thumbnails - crossfade
  document.getElementById("thumbs").addEventListener("click", e=>{
    const t = e.target.closest("[data-i]");
    if(!t) return;
    activeImg = +t.dataset.i;
    const mainImg = document.getElementById("mainImg");
    mainImg.style.opacity = 0;
    setTimeout(()=>{
      mainImg.src = p.images[activeImg];
      mainImg.style.opacity = 1;
    }, 150);
    document.querySelectorAll(".pdp__thumb").forEach(th=>th.classList.toggle("active", th===t));
  });
*** End Patch

*** Begin Patch
*** Update File: C:\Users\mfabid\My stack\projects\ZCodeProject\shared.js
*** Find:
/* ---------- GLOBAL CLICK DELEGATION (wishlist + add-to-cart on cards) ---------- */
document.addEventListener("click", e=>{
  // add to bag (from card overlay button)
  const add = e.target.closest("[data-add]");
  if(add){
    e.preventDefault();
    addToCart(+add.dataset.add);
    return;
  }
  // wishlist toggle
  const wish = e.target.closest("[data-wish]");
  if(wish){
    e.preventDefault();
    wish.classList.toggle("active");
    wish.textContent = wish.classList.contains("active") ? "♥" : "♡";
  }
});
*** Replace:
/* ---------- GLOBAL CLICK DELEGATION (wishlist + add-to-cart on cards) ---------- */
document.addEventListener("click", e=>{
  // add to bag (from card overlay button)
  const add = e.target.closest("[data-add]");
  if(add){
    e.preventDefault();
    addToCart(+add.dataset.add);
    return;
  }
  // wishlist toggle
  const wish = e.target.closest("[data-wish]");
  if(wish){
    e.preventDefault();
    wish.classList.toggle("active");
    wish.textContent = wish.classList.contains("active") ? "♥" : "♡";
  }
});

/* ---------- VIEW TRANSITIONS (Task 4) ---------- */
if (document.startViewTransition) {
  document.addEventListener("click", e => {
    const link = e.target.closest("a[href]");
    if (!link) return;
    const href = link.getAttribute("href");
    if (!href) return;
    if (href.startsWith("#") || href.startsWith("mailto:") || href.startsWith("tel:")) return;
    try {
      const url = new URL(href, location.origin);
      if (url.origin !== location.origin) return;
    } catch { return; }
    e.preventDefault();
    document.startViewTransition(() => { location.href = href; });
  });
}
*** End Patch